{
  "name": "immutablestack",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build_src": "tsc && esbuild generators/gen_controller.ts --bundle --minify --sourcemap --outfile=gen.js --platform=node",
    "build_hbrew":  "tar -czvf ImmutableStack-v0.0.16.tar.gz . && mv -f ImmutableStack-v0.0.16.tar.gz ~/git/homebrew-immutable",
    "build": "npm run build_src && npm run build_hbrew"
  },
  "keywords": [],
  "author": "macioa",
  "license": "Apache-2.0",
  "description": "Toolkit for working with the Immutable Stack (elixir/phoenix + react/redux)",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "esbuild": "^0.23.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
